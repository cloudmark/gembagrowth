
'use client';

import { Button } from '@/components/ui/button';

export default function HeroButtons() {
    return (
        <div className="mt-8 flex flex-col sm:flex-row items-center justify-center gap-4">
            {/* The "Let's Connect" button was previously here. */}
        </div>
    );
}
